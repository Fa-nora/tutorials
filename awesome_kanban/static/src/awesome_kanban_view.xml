<?xml version="1.0" encoding="UTF-8"?>

<templates xml:space="preserve">

    <!-- Customer List -->
    <t t-name="awesome_kanban.CustomerList">
    <div class="o_awesome_customer_list">

        <div class="o_awesome_customer_title">
            Customers
        </div>

        <!-- Search -->
        <div class="o_awesome_customer_search">
            <input
                type="text"
                placeholder="Search..."
                t-model="state.searchString"
            />
        </div>

        <!-- Active customers -->
        <div class="o_awesome_customer_filter">
            <input
                type="checkbox"
                id="active_customers"
                t-model="state.displayActiveCustomers"
            />

            <label for="active_customers">
                Active customers
            </label>
        </div>

        <!-- Customers -->
        <t
            t-foreach="displayedCustomers"
            t-as="customer"
            t-key="customer.id"
        >
            <div
                class="o_awesome_customer"
                t-on-click="() => props.selectCustomer(customer)"
            >
                <t t-esc="customer.name"/>
            </div>
        </t>

    </div>
</t>

    <!-- Kanban View -->
    <t
        t-name="awesome_kanban.AwesomeKanbanView"
        t-inherit="web.KanbanView"
        t-inherit-mode="primary"
    >

        <xpath
            expr="//t[@t-component='props.Renderer']"
            position="replace"
        >

            <div class="o_awesome_kanban_container">

                <CustomerList
                    selectCustomer.bind="selectCustomer"
                />

                <div class="o_awesome_kanban_content">

                    <t
                        t-component="props.Renderer"
                        t-if="model.isReady"
                        list="model.root"
                        archInfo="props.archInfo"
                        Compiler="props.Compiler"
                        readonly="props.readonly"
                        forceGlobalClick="props.forceGlobalClick"
                        deleteRecord.bind="deleteRecord"
                        openRecord.bind="openRecord"
                        noContentHelp="props.info.noContentHelp"
                        scrollTop.bind="scrollTop"
                        canQuickCreate="canQuickCreate"
                        quickCreateState="quickCreateState"
                        progressBarState="progressBarState"
                    />

                </div>

            </div>

        </xpath>

    </t>

</templates>